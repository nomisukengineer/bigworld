<% provide(:title, 'メンズ') %>
<h1>メンズ</h1>

<%= link_to "Mens", "/products/mens", class: 'btn btn-default'  %>
<%= link_to "Ladies", "/products/ladies", class: 'btn btn-default'  %>

<!--  検索拡張機能  -->
<p>商品の検索</p>
<%= form_tag(products_path, method: :get) do %>
  <%= text_field_tag :search %>
  <%= submit_tag 'Search', :name => nil %>
<% end %>

<main>
<div class="content-area">

  <% @mens_products.each do |product| %>
    <div class="content">

      <table>
        <tr>
        <%= link_to image_tag("00300-ACT_133_F.png", alt: "T-shirt"),
            product %>
            <th>
            <%= link_to product.product_name, product %>
        <tr>
            <td>ジャンル <%= product.category.category_name %></td>
        <tr>
            <td>￥<%= product.price %></td>
        </tr>
      </table>
    </div>
  <% end %>
 </div>

</main>
